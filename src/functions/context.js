import { createContext } from "react";
const EnKey = createContext('')
const EnText = createContext('')

const DeKey = createContext('')
const DeText = createContext('')


export { EnKey , EnText , DeKey , DeText } 